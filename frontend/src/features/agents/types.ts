export interface Agent {
  _id: string
  props: {
    agent_id: string
    afrimoney: boolean
    area: string
    city: string
    first_name: string
    last_name: string
    phone: string
    province: string
    status: string
    zone: string
  }
}
